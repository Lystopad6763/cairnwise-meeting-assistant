# Cairnwise SPA environment
# Base URL of the FastAPI backend (no trailing slash).
VITE_API_BASE=http://localhost:8000

# Optional client-side upload size cap in MB (default 1024).
VITE_MAX_UPLOAD_MB=1024
